import React from "react";
import { LoadingContainer, LoadingStyled } from "./styles";

const Loading = () => {
  return (
    <LoadingContainer>
      <LoadingStyled />
    </LoadingContainer>
  );
};

export default Loading;
